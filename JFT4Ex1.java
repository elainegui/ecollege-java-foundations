package eCollegeJava;

public class JFT4Ex1 {

	public static void main(String[] args) {
		// exercise1
		// output in ms-dos console

		// click mouse right button in the file to compile
		// select properties
		// click show in system explorer
		// click once in the blank part of the path of the file explorer, that will
		// select all
		// type in cmd
		// it will open the terminal with the src/package path
		// to compile we type javac package\fileName.java
		// then dir to see the fileName.class formed

		// obs: it won't run java command if the java file is in a package
		/*so, we have to say the fully qualified name of the file
		which is:package.fileName
		*/
		// so, in cmd prompt we type cd..
		// now it will show from src
		// then we type java package.fileName
		// the output if there is any, will show

	}

}
