package eCollegeJava;

public class JFT14Ex1 {

	public static void main(String[] args) {
		String value = "two";
		int parsedValue = 0;

		//	try {
		/*if (!((value.getClass()).equals(Integer.class))) {
			System.out.println("Caught by if statement. Unable to parse data.");
		} else {
			parsedValue = Integer.parseInt(value);
		}*/

		//	} catch (NumberFormatException e) {
		//System.out.println("Error. Unable to parse data. Program closing.");
		//}
	}

	/*Consider the following question. Should we have written a try catch block to handle the
	NumberFormatException or could it have been avoided. If so, how?*/

	//Instead of the try/catch block, we could use if statement

}
